<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="15dp">

    <ImageView
        android:id="@+id/imageViewBackgroundBanner"
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:scaleType="fitXY" />
    <RelativeLayout
        android:background="#60000000"
        android:id="@+id/relative1"
        android:layout_alignBottom="@+id/imageViewBackgroundBanner"
        android:layout_width="200dp"
        android:layout_height="wrap_content">

        <ImageView
            android:id="@+id/imageSongQuangcao"
            android:layout_width="50dp"
            android:layout_height="50dp"
            android:layout_marginHorizontal="10dp"
            android:layout_marginBottom="10dp"
            android:scaleType="fitXY"
            android:padding="2dp" />
        <TextView
            android:maxLines="1"
            android:ellipsize="end"
            android:id="@+id/tvTitleQuangcao"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_toRightOf="@+id/imageSongQuangcao"
            android:text="Phía sau một cô gái"
            android:textColor="@color/white"
            android:textSize="12sp"
            android:textStyle="bold" />
    </RelativeLayout>
</RelativeLayout>
